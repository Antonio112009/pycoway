version: 2
updates:
  # Keep the workflow actions on their newest releases. PRs target
  # development because only that branch may open PRs against main.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    target-branch: development
